# database/seed

Demo users are created by `backend/scripts/seed_demo_users.py`:

```bash
cd backend
PYTHONPATH=. .venv/bin/python scripts/seed_demo_users.py
```

Put additional SQL/CSV fixtures for demo alerts and replay scenarios here.

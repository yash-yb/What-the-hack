# ai/feature_engineering

Per-window feature calculators and the schema validator.

- `validate_feature_schema.py` — standalone validator and stress test for
  `docs/api/feature_schema_contract.json`. Run from the repository root:

  ```bash
  python3 ai/feature_engineering/validate_feature_schema.py
  ```

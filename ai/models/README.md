# ai/models

Trained model artifacts live here locally but are **not committed** (see `.gitignore`).
Commit a small `<model_name>.metadata.json` next to each artifact with: model name, type,
dataset, feature-set version, metrics, and training date. That metadata feeds the
`model_versions` table.
